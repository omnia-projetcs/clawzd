"""Clawzd — Output compressor (re-export shim).

This module has been moved to ``app.core.output_compressor``.
This shim exists for backward compatibility — import from ``app.core.output_compressor`` in new code.
"""
from app.core.output_compressor import *  # noqa: F401,F403
